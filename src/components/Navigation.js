import React from 'react';

const Navigation = props => {
    return (
        <nav>
            <h1 className = "logo">greenpaws <i className = "paws"></i></h1>
        </nav>
    )
}
export default Navigation;